spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis_plus?useSSL=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    password: root
    username: root
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  #设置mybatis-plus全局配置
#  global-config:
#    db-config:
      #设置实体类表对应的统一前缀
#      table-prefix:
      #设置统一的主键生成策略
    #  id-type: auto
  type-aliases-package: com.sinn.pojo
  #扫描通用枚举包
  type-enums-package: com.sinn.enums